{
  "name": "@platforma-open/milaboratories.vj-usage.workflow",
  "version": "2.0.1",
  "type": "module",
  "description": "Block Workflow",
  "scripts": {
    "build": "rm -rf dist && pl-tengo check && pl-tengo build",
    "test": "vitest",
    "format": "/usr/bin/env emacs --script ./format.el"
  },
  "dependencies": {
    "@platforma-open/milaboratories.vj-usage.software": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:",
    "@platforma-open/milaboratories.software-ptransform": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "vitest": "catalog:"
  }
}
